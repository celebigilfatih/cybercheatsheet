MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>/<dbName>?retryWrites=true&w=majority
SEED_SECRET=change-this-secret

# Yerel/Docker için örnekler:
# Mac/Windows Docker içinde host Mongo'ya bağlanmak için:
# MONGODB_URI=mongodb://host.docker.internal:33017/cybersheet
# Alternatif: URI sağlamazsanız aşağıdaki env'lerle otomatik oluşturulur:
# MONGODB_HOST=host.docker.internal
# MONGODB_PORT=33017
# MONGODB_DB=cybersheet

# Opsiyonel auth için (kullanıyorsanız):
# JWT_SECRET=your-jwt-secret